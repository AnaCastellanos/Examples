# How to run this?

## Example One

Just open a terminal and type the following:

`cd Example_one`

`npm install`

`node app`


## Example two

Just open a terminal and type the following:

`cd Example_two`

`npm install`

`node app`
